# Markdown Preview Style

`markdown-style.css` is consumed remotely via VS Code's `markdown.styles` setting — add the url to any machine's user `settings.json`:

```json
"markdown.styles": [
    "https://raw.githubusercontent.com/bilda-one/cdn-ish/main/css/markdown-preview/markdown-style.css"
]
```
